<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <p style="display: flex; justify-content: center; align-items: center; color: red; font-weight: bolder;">
        Error In Uploading ! 
        <br>
        Please Upload Correct "File Type" and Size Should Be "Lesser" Than "3MB"
    </p>
    <form action="/login" method="GET"
            style="display: flex; justify-content: center; align-items: center; color: black; font-weight: bolder; font-style: italic ;">
            <button type="submit">Login Again</button>
        </form>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error Page</title>
    <style>
        /* Body styling */
        body {
            margin: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: #1c1c1c; /* Deep charcoal background */
            position: relative;
            overflow: hidden;
            color: #f8f3d4; /* Soft gold text color */
            font-family: Arial, sans-serif;
        }

        /* Error message styling */
        p {
            font-weight: bold;
            text-align: center;
            font-size: 1.6em;
            background-color: rgba(50, 50, 50, 0.8); /* Charcoal overlay */
            padding: 20px 40px;
            border-radius: 10px;
            color: #f8f3d4; /* Soft gold color for text */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
            z-index: 1;
            margin-bottom: 20px;
            animation: fadeIn 1s ease-out;
        }

        /* Button styling */
        button {
            padding: 15px 30px;
            background-color: #b2912f; /* Gold button color */
            color: #1c1c1c; /* Charcoal text on button */
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
            font-weight: bold;
            transition: background-color 0.3s, transform 0.3s;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            z-index: 1;
        }

        button:hover {
            background-color: #e3c16f; /* Lighter gold on hover */
            transform: scale(1.05);
        }

        /* Fade-in animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Animation for floating balls */
        .ball {
            position: absolute;
            border-radius: 50%;
            opacity: 0.5;
            background-color: #f8f3d4; /* Soft gold color for all balls */
            animation: float 6s infinite;
            z-index: 0;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        /* Ball sizes */
        .ball1 { width: 30px; height: 30px; }
        .ball2 { width: 20px; height: 20px; }
        .ball3 { width: 40px; height: 40px; }
        .ball4 { width: 25px; height: 25px; }
        .ball5 { width: 30px; height: 30px; }
    </style>
</head>
<body>
    <!-- Error message -->
    <p>
        Error In Uploading! <br>
        Please Upload Correct "File Type" and Size Should Be "Lesser" Than "3MB"
    </p>

    <!-- Retry button -->
    <form action="/login" method="GET">
        <button type="submit">Login Again</button>
    </form>

    <!-- Animated balls -->
    <div class="ball ball1" style="top: 10%; left: 20%; animation-duration: 5s;"></div>
    <div class="ball ball2" style="top: 30%; left: 50%; animation-duration: 7s;"></div>
    <div class="ball ball3" style="top: 60%; left: 80%; animation-duration: 6.5s;"></div>
    <div class="ball ball4" style="top: 80%; left: 30%; animation-duration: 6s;"></div>
    <div class="ball ball5" style="top: 20%; left: 70%; animation-duration: 5.5s;"></div>
</body>
</html>
