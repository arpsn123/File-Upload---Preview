<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <ul>
        <% if(reg_users && reg_users.length>0){%>
            <% reg_users.forEach(i=>{ %>
                <li>
                    <p>Name : <%= i.fullname %>
                    </p>
                    <p>Username : <%= i.username %>
                    </p>
                    <p>email : <%= i.email %>
                    </p>
                    <p>mobile : <%= i.mobile %>
                    </p>
                </li>
                <%})%>
                    <%}else{%>
                        <p>Looks Like You Are The First One To Register...</p>

                        <%}%>
    </ul>
</body>

</html> -->



<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Georgia', serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: #1b1f24; /* Deep rich charcoal */
            color: #e1e1e1;
        }

        .container {
            width: 90%;
            max-width: 800px;
            background: #2a2d34; /* Dark slate for the container */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.4);
            overflow: hidden;
        }

        .header {
            text-align: center;
            font-size: 2em;
            margin-bottom: 15px;
            color: #d4af37; /* Gold for a luxurious feel */
            border-bottom: 2px solid #3b3e47;
            padding-bottom: 10px;
            font-weight: bold;
            animation: fadeInDown 1.2s ease;
        }

        .user-list {
            max-height: 450px;
            overflow-y: auto;
            padding: 10px;
        }

        .user-list::-webkit-scrollbar {
            width: 8px;
        }

        .user-list::-webkit-scrollbar-track {
            background: #3b3e47; /* Darker track to match the scheme */
            border-radius: 8px;
        }

        .user-list::-webkit-scrollbar-thumb {
            background: #d4af37; /* Gold scrollbar */
            border-radius: 8px;
        }

        .user-item {
            background: #343a40; /* Dark slate-gray for user cards */
            border: 1px solid #52575d;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 10px;
            transition: transform 0.3s ease, background 0.3s ease;
            position: relative;
            animation: fadeIn 1s ease forwards;
            color: #e1e1e1;
        }

        .user-item:hover {
            transform: translateY(-3px);
            background: #2f333a; /* Slightly lighter shade on hover */
        }

        .user-item p {
            margin: 5px 0;
            color: #d4af37; /* Gold text for labels */
        }

        .user-item p strong {
            color: #e1e1e1; /* Light gray for the actual data */
        }

        .no-users {
            text-align: center;
            padding: 20px;
            color: #b8b8b8;
            font-style: italic;
            font-size: 1.1em;
            animation: fadeIn 2s ease;
        }

        /* Keyframes for animations */
        @keyframes fadeInDown {
            0% { opacity: 0; transform: translateY(-20px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        @keyframes fadeIn {
            0% { opacity: 0; transform: translateY(10px); }
            100% { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>

<body>
    <div class="container">
        <h2 class="header">Registered Users</h2>
        <div class="user-list">
            <% if (reg_users && reg_users.length > 0) { %>
                <% reg_users.forEach(i => { %>
                    <div class="user-item">
                        <p><strong>Name:</strong> <%= i.fullname %></p>
                        <p><strong>Username:</strong> <%= i.username %></p>
                        <p><strong>Email:</strong> <%= i.email %></p>
                        <p><strong>Mobile:</strong> <%= i.mobile %></p>
                    </div>
                <% }) %>
            <% } else { %>
                <p class="no-users">Looks like you are the first one to register...</p>
            <% } %>
        </div>
    </div>
</body>

</html>

