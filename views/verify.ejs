<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OTP Verification</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f9f9f9;
    }

    .otp-container {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .otp-input {
      width: 200px;
      height: 40px;
      text-align: center;
      font-size: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
      outline: none;
      margin-bottom: 20px;
    }

    .otp-input:focus {
      border-color: #007bff;
    }

    button {
      padding: 10px 20px;
      font-size: 16px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="otp-container">
    <h2>Enter Your 8-Digit OTP</h2>
    <form action="/verify" method="POST">
      <input type="text" name="code" class="otp-input" maxlength="8" required>
      <button type="submit">Verify OTP</button>
    </form>
  </div>
</body>
</html> -->





<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f9f9f9;
            margin: 0;
            overflow: hidden;
            position: relative; /* Required for positioning the balls */
        }

        .otp-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: rgba(255, 255, 255, 0.9); /* Slight transparency */
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            z-index: 2; /* Ensure it's on top of the balls */
            animation: fadeIn 1s; /* Animation for appearance */
        }

        h2 {
            color: #333;
            margin-bottom: 20px;
            text-align: center;
        }

        .otp-input {
            width: 200px;
            height: 50px;
            text-align: center;
            font-size: 24px;
            border: 2px solid #ccc;
            border-radius: 5px;
            outline: none;
            margin-bottom: 20px;
            transition: border-color 0.3s; /* Smooth border transition */
        }

        .otp-input:focus {
            border-color: #007bff; /* Highlight on focus */
        }

        button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s; /* Smooth transition */
        }

        button:hover {
            background-color: #0056b3; /* Darker blue on hover */
            transform: scale(1.05); /* Scale effect on hover */
        }

        @keyframes fadeIn {
            from {
                opacity: 0; /* Start transparent */
                transform: translateY(-20px); /* Slide in from above */
            }
            to {
                opacity: 1; /* Fully visible */
                transform: translateY(0); /* End position */
            }
        }

        /* Ball styles */
        .ball {
            position: absolute;
            border-radius: 50%;
            animation: bounce 2s infinite; /* Bouncing animation */
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-100px); /* Bounce height */
            }
        }

        /* Ball color and size */
        .ball1 {
            width: 30px;
            height: 30px;
            background-color: #ff5733; /* Red */
            left: 20%;
            animation-delay: 0s; /* First ball */
        }

        .ball2 {
            width: 40px;
            height: 40px;
            background-color: #33c1ff; /* Blue */
            left: 50%;
            animation-delay: 0.2s; /* Second ball */
        }

        .ball3 {
            width: 50px;
            height: 50px;
            background-color: #8cff33; /* Green */
            left: 80%;
            animation-delay: 0.4s; /* Third ball */
        }
    </style>
</head>
<body>
    <div class="otp-container">
        <h2>Enter Your 8-Digit OTP</h2>
        <form action="/verify" method="POST">
            <input type="text" name="code" class="otp-input" maxlength="8" required>
            <button type="submit">Verify OTP</button>
        </form>
    </div>

    <!-- Bouncing balls -->
    <div class="ball ball1"></div>
    <div class="ball ball2"></div>
    <div class="ball ball3"></div>
</body>
</html>
