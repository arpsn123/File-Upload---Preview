<!-- <!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <h3
            style="display: flex; justify-content: center; align-items: center; color: blue; font-weight: bolder;">
            Enter Your Credentials :
        </h3>
        <div class="container"
            style="display: flex; justify-content: center; align-items: center; color: green; ">


            <form action="/login" method="POST">
                Username :
                <input type="text" name="username" required>
                Password :
                <input type="text" name="password" required>
                <button type="submit"> Submit</button>
            </form>
        </div>
    </body>
</html> -->




<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        /* Body styles */
        body {
            margin: 0;
            height: 100vh;
            font-family: 'Georgia', serif;
            display: flex;
            justify-content: center;
            align-items: center;
            background: #1a1a1a; /* Deep charcoal background */
            overflow: hidden;
            position: relative;
            color: #d4af37; /* Gold accents for text */
        }

        /* Container styling */
        .container {
            background: rgba(34, 34, 34, 0.95); /* Slightly transparent dark container */
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
            text-align: center;
            z-index: 1; /* Ensure it's above the animated balls */
            width: 300px;
        }

        /* Header styles */
        h3 {
            color: #d4af37;
            margin-bottom: 20px;
            font-size: 1.2em;
            font-weight: bold;
        }

        /* Input field styles */
        input {
            width: 100%;
            padding: 8px;
            margin: 8px 0;
            border: 1px solid #555;
            border-radius: 5px;
            outline: none;
            background: #333;
            color: #d4af37;
            transition: border 0.3s ease;
        }

        input:focus {
            border-color: #d4af37;
        }

        /* Button styles */
        button {
            padding: 10px;
            background-color: #d4af37;
            color: #1a1a1a;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            width: 100%;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #b58b2b;
        }

        /* Animation styles for balls */
        .ball {
            position: absolute;
            border-radius: 50%;
            opacity: 0.6;
            animation: float 4s infinite;
        }

        /* Keyframes for ball animation */
        @keyframes float {
            0% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
            100% {
                transform: translateY(0);
            }
        }

        /* Ball colors and sizes */
        .ball1 { width: 25px; height: 25px; background: #d4af37; animation-duration: 3s; }
        .ball2 { width: 40px; height: 40px; background: #a18c5f; animation-duration: 4s; }
        .ball3 { width: 30px; height: 30px; background: #8c7851; animation-duration: 2.5s; }
        .ball4 { width: 20px; height: 20px; background: #d4af37; animation-duration: 3.5s; }
        .ball5 { width: 50px; height: 50px; background: #a18c5f; animation-duration: 3s; }
    </style>
</head>
<body>
    <!-- Animated Balls -->
    <div class="ball ball1" style="top: 15%; left: 25%;"></div>
    <div class="ball ball2" style="top: 40%; left: 60%;"></div>
    <div class="ball ball3" style="top: 70%; left: 75%;"></div>
    <div class="ball ball4" style="top: 80%; left: 40%;"></div>
    <div class="ball ball5" style="top: 25%; left: 85%;"></div>

    <div class="container">
        <h3>Enter Your Credentials:</h3>
        <form action="/login" method="POST">
            <label for="username">Username:</label>
            <input type="text" name="username" required placeholder="Username" />
            <label for="password">Password:</label>
            <input type="password" name="password" required placeholder="Password" />
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>

